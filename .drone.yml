workspace:
  base: /go
  path: src/github.com/Oppodelldog/checkredirects

pipeline:
  build:
    image: golang:1.9.4
    commands:
      - go get -u github.com/golang/dep/cmd/dep
      - dep ensure
      - go get ./...
      - go get github.com/mattn/goveralls
      - goveralls -service drone.io -repotoken HPOUTBK8d2NyOXBGTlskUWIOTzQGCfgWW
      - go build

matrix:
 GO_VERSION:
   - latest
   - "1.9"